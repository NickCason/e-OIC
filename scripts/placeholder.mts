// Placeholder so tsconfig.node.json has at least one input file before Plan D
// renames vite.config.js -> vite.config.ts and adds scripts/**/*.mts. Delete
// this file in Plan D once real .mts scripts exist.
export {};
